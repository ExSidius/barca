[project]
name = "bench-barca"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["barca"]

[tool.uv.sources]
barca = { path = "../../.." }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
