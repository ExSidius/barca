[project]
name = "barca-workspace"
version = "0.1.0"
description = "Barca — minimal asset orchestrator"
requires-python = ">=3.13"

[tool.uv]
dev-dependencies = [
    "barca",
    "barca-cli",
    "barca-server",
    "pytest>=8.0",
    "scikit-learn>=1.0",
    "niquests>=3.0",
]

[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
barca = { workspace = true }
barca-cli = { workspace = true }
barca-server = { workspace = true }

[tool.ruff]
line-length = 200
