# git-cliff configuration
# https://git-cliff.org/docs/configuration

[changelog]
header = "# Changelog\n\nAll notable changes to this project will be documented in this file.\n"
body = """
{% if version %}\
    ## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
    ## [unreleased]
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
    ### {{ group | striptags | trim | upper_first }}
    {% for commit in commits %}
        - {{ commit.message | split(pat="\n") | first | upper_first }}\
    {% endfor %}
{% endfor %}\n
"""
trim = true
render_always = false

[git]
conventional_commits = true
filter_unconventional = false
split_commits = false
commit_preprocessors = [
    # Strip Co-Authored-By trailers
    { pattern = '\n\nCo-Authored-By:.*', replace = "" },
]
commit_parsers = [
    { message = "^[Mm]erge", skip = true },
    { type = "feat", group = "Features" },
    { type = "fix", group = "Bug Fixes" },
    { type = "refactor", group = "Refactor" },
    { type = "polish", group = "Polish" },
    { type = "doc", group = "Documentation" },
    { type = "test", group = "Testing" },
    { type = "perf", group = "Performance" },
    { type = "remove", group = "Removed" },
    { type = "release", group = "Release" },
    { message = ".*", group = "Changes" },
]
filter_commits = false
tag_pattern = "v[0-9].*"
sort_commits = "newest"
topo_order_commits = true
